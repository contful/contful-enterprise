# =============================================================================
# Contful Docker 环境配置
# 此文件由项目根目录的 .env 自动复制而来
# 运行前请确保已设置根目录的 .env 文件
# =============================================================================

# 从项目根目录 .env 复制（不要直接修改此文件）
# cp .env docker/.env
